# amqp source
# FIXME aconway 2015-11-20: find a safe port?
<source>
  @type amqp_qpid
  @id fluent-amqp
  url localhost:5672/fluent-test
  tag stdout.amqp-test
</source>

<match stdout.**>
  @type stdout
</match>

<match fluent.**>
  @type stdout
</match>

## match not matched logs and write to file
#<match **>
#  @type file
#  path /var/log/fluent/else
#  compress gz
#</match>
