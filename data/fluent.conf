# TODO: Include configuration for forwarding of logs from the container so that
#       each image based on this one also monitors itself.

@include config.d/forward/forward.conf
@include config.d/local/*.conf

